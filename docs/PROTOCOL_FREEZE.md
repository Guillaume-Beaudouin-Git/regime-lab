# Protocol freeze

On data everyone has already seen, pre-registration cannot mean "data unseen".
It means **the protocol was fixed before the results were looked at**, and that
the fixing is verifiable by a third party. That is what this file records.

## Frozen document

`docs/CHARTER.html` — charter v2, 8 September 2026.

```
SHA-256  5791c1887c41d1f6b0449bafad2125013b82dc6b3506b4d72564e14ec1abc0db
```

Verify with:

```bash
shasum -a 256 docs/CHARTER.html
```

The charter is never amended in place. Revisions go in the amendment log below,
each dated and justified, so that a reader can see what changed and when.

## What the freeze commits us to

- The decisive test (T2) as specified: dynamic ex-ante volatility targeting,
  target calibrated on training folds only, paired block bootstrap, minimum
  detectable effect reported, metrics that are not scale-invariant.
- The two base strategies, fixed before any regime is estimated: 60/40, and an
  equal-risk multi-asset momentum book.
- The three stopping rules and the three admissible outcomes.
- Transaction costs in basis points, written before results.

## Amendment log

| date | version | change | reason |
| --- | --- | --- | --- |
| 2026-09-08 | state ordering | **APPLIED** — states are ranked by the volatility realised in each on the training window | A sign error in this repository's wrapper, not a property of the reference implementation. See the correction below. |
| 2026-09-10 | audit corrections | **APPLIED** — NBER scored against the calendar month rather than through a 45-day publication lag; a causal volatility-quantile placebo added to every table; marginal information reported on forward volatility as well as forward returns | Found by an independent audit. Each overstated or omitted a result; the corrected figures are lower and are the ones published. |
| 2026-09-08 | sizing rule | **ADDED** — a graded inverse-volatility sizing rule alongside the frozen on/off rule; the on/off rule is still reported | The frozen rule is a timing rule and the measured signal separates variance, not mean. Adding the rule the evidence supports is disclosed here rather than substituted quietly, and the original stays in every table. |
| 2026-09-08 | grid | **DECLINED** — the jump-penalty grid was not widened despite the optimum landing on its floor | Extending a grid after seeing which end wins is an amendment made in the light of results. Recorded in docs/CALIBRATION_NOTES.md. |
| 2026-09-08 | v1 → v2 | T2 respecified; families D cut; strategies frozen; power section added; data coverage corrected | Adversarial review found T2 was scale-invariant, hence vacuous on Sharpe, and that deferring the strategy choice to phase 4 was circular. |
| 2026-09-13 | T1, T3, T5 | **APPLIED** — the three controls the charter promised and `RESULTS_FINAL.md` declared missing are implemented and run: `scripts/run_t1_control.py`, `run_t3_control.py`, `run_t5_refit.py` + `run_t5_control.py`. Results in `docs/RESULTS_FALSIFICATION.md` | A promise in a frozen, publicly tagged document is an obligation, not an option. They were implemented after the primary results were known, which is disclosed here: each was specified from the charter text before its own output was read, and each reports the outcome it produced rather than the outcome that would have been convenient. T1 and T3 weaken the study's position; they are published unchanged. |
| 2026-09-13 | five folds | **APPLIED** — the five declared folds are evaluated for the first time: fold fixed effects, leave-one-fold-out, Cochran's Q. Results in `docs/RESULTS_FOLDS.md` | `walk_forward` had one call site and was used only to read the first fold's start date. The central result survives all three checks; the weakest leave-one-out cell is 1.74 points at t −2.42, dropping the financial crisis. |
| 2026-09-13 | walk-forward of record | **APPLIED** — the executed protocol (one expanding block, 49 semi-annual refits, online prediction) is declared the walk-forward of record; the five-fold slice is retained as a dispersion diagnostic, not as the estimator | Not a preference. Two of A′ sparse jump's five folds contain a **single state across 1,279 trading days**, and one of A jump's does, so the charter's "three folds of five" stopping rule is arithmetically uncomputable for the families the study leads with. An object with a 456-day mean run length cannot be cut into five five-year windows and still vary inside each one. Measured in `docs/RESULTS_FOLDS.md` §2. |
| 2026-09-13 | heterogeneity p-values | **WITHDRAWN** — the fold-interaction Wald test on all five folds, which reported A′ sparse jump at p = 4.5 × 10⁻⁵ | Not identified. With two degenerate folds the design matrix has rank 9 for 11 columns in A′ and 10 for A jump. Superseded by `scripts/run_fold_heterogeneity.py`, which checks rank first and reports A′ at p 0.218. The rank-deficient figure was found and retracted by the same work that produced it. |
| 2026-09-21 | M1 scope | **NARROWED** — the calendar realignment applies to **eight** spot FX series (AUDUSD, CHF, EURUSD, GBPUSD, JPY, NOK, NZDUSD, SEK), not the ten the pre-registration locks. **CAD=X and MXN=X are aligned and must not be shifted.** Measured by `scripts/audit_fx_alignment.py` | The pre-registration's D3(a) verified three series against a future and *inferred* the remaining seven from a shared data endpoint. The inference is wrong on two of them. Two independent tests were run, neither needing data this repository lacks: dating against shocks with a settled calendar date, and a lead-lag against the ICE dollar index with `^GSPC` as an aligned control. They agree wherever both are decisive, and never disagree. MXN records its 8.30% move on the correct session of the 2016 US election, 5.8 sigma, and its same-day correlation with the dollar index is four times its lagged one; CAD reads aligned on the lead-lag test with no decisive event available. Applying the locked M1 to all ten would have **introduced** into CAD and MXN the exact defect M1 exists to remove. The narrowing was measured before the repaired book produced any return, and it reduces the modification rather than extending it. |
| 2026-09-22 | Shu replication row withdrawn | **WITHDRAWN** — the "penalty by cross-validation" row of `docs/REPLICATION_SHU2024.md` (8.6% / 13.0% / 0.50 / −32.0% / 76.6% / 112%). The missing arm was written rather than the row retracted unexamined, and what it produces is published beside the withdrawal | Fourth instance of the programme's signature failure: no committed code produced the row, `main()` having swept eight fixed penalties none of which reads it. `select_penalty` now re-chooses on each refit's training window, scoring candidates on a held-out block by the excess-return separation the model is for. It gives **7.8% / 10.2% / 0.54 / −25.0% / 63.6% / 762%** — and the turnover is the tell, because the criterion lands on λ = 0 on 44 of 90 refits and λ = 0 turns over 1448% a year. A criterion indifferent to flipping buys separation with turnover. **The search stopped there on purpose:** "by cross-validation" does not say cross-validated on what, so the criterion is underdetermined by the document, and trying criteria until one returns 112% would be fitting the procedure to a remembered answer. The sweep, the buy-and-hold and the paper's conclusion are untouched; they never rested on this row. |
| 2026-09-22 | second base strategy | **DECLARED, not wired, not deleted** — `equal_risk_momentum` keeps its definition and its absence of a call site, and the reason is written into its docstring | The charter promises two frozen base strategies and reports both whatever happens, so deleting it drops a promise. Wiring it is not honest either: two of the six panel series are **rates quoted in per cent** and `pct_change()` on a rate is not a return, an error worth **+0.176 of Sharpe** (0.337 naive against 0.513 with a proper bond return) — larger than most effects this programme has measured — and six instruments is a fifth of the N ≥ 30 the programme requires for a cross-sectional claim. Choosing between two constructions that differ by more than the quantity under test, after seeing which reads higher, is the adjustment the protocol forbids. |
| 2026-09-22 | dormant decomposition published | **PUBLISHED** — `docs/RESULTS_DECOMPOSITION.md`. The four attributions the charter names were computed by `scripts/run_decomposition.py`, ran cleanly, and appeared in no document | The result is the programme's central finding in its cleanest form and it was sitting uncomputed: the rungs needing no regime model carry the value (volatility timing **+0.44%**), while the regime model's own contribution is **−0.04% with hindsight** and **−0.25% net of latency**, before **−0.14%** of costs. Hindsight is nearly free — replacing the perfect-foresight state with the filtered one costs 0.02 to 0.10 of Sharpe and exactly 0.00 for two families — which is not a signal degraded by latency but a signal with little to degrade. |
| 2026-09-22 | Shiller workbook untracked | **APPLIED** — `git rm --cached sh.xls`, `*.xls` added to `.gitignore`, history **not** rewritten | It is Shiller's `ie_data.xls`, 67.2% of the public repository by bytes, with no reader: `scripts/replicate_shu2024.py` reads the URL and never the file. The URL was tested rather than assumed and answers **HTTP 200** on 2026-09-22, so the local copy is a convenience and not the only one — which is what the coherence report left open. A force-push over a published tag costs more than 1.6 MB of blob, the same reasoning applied to the anonymisation. |
| 2026-09-22 | undeclared and undocumented dependencies | **APPLIED** — `xlrd` declared in `pyproject.toml`; `regime-lab` declared as a path dependency in both dependent projects with a `[tool.uv.sources]` entry; `scripts/fetch_trend_universe.py` documented as the hard prerequisite of `run_extensions.py`, which now fails with an instruction rather than a missing path | Three ways a fresh clone died silently. `pandas.read_excel` on an `.xls` needs an engine that was installed by hand, invisible to the tests because `main()` is never called by them. The two derived projects imported `regime_lab` through an editable install declared nowhere, which is why moving the programme folder broke all three venvs twice. And nothing but `fetch_trend_universe.py` writes `trend_universe.parquet`, which no execution order mentioned. |
| 2026-09-22 | sign map now actually tested | **APPLIED** — `macro-momentum/tests/test_sign_map.py`, eight tests | `macro_momentum/exposures.py` claimed since it was written that "the test suite checks that" the code map matches `docs/PRESPEC.md`. `tests/` was empty. The new test **parses the frozen table** rather than restating its values, because a copy of the numbers would agree with itself forever, and it pins the two cells whose reversal would be most tempting — H1 was falsified on that map, and reversing it after the fact would have turned −0.38 into +0.32. |
| 2026-09-23 | Two Sigma study | **OPENED and LOCKED** — `docs/PRESPEC_TWOSIGMA.md`, locked by the commit that adds it, against build `3b64ab2` (the build commit `22abbe0` plus the audit commit). A context partition (K-means, K = 4, twenty features orthogonalised on log realised volatility, refitted walk-forward, labelled out of sample, lagged one session) used as a **selector across a ten-signal library on the 49 US industries**. Six primary locks over three levels (A first moment, B second moment, C rebalancing cadence), Holm over the six, 20 declared evaluations (6 primaries, 14 sensitivities that never found a PASS). **No statistic of the escalation tree was read before the lock**: no mean, Sharpe, IC, hit rate, alpha, `m_ik`, per-state quantity of any signal return or covariance-forecast loss was computed | The draft (`pilotage/plans_de_recherche/twosigma/PRESPEC_TWOSIGMA.md`, 2026-09-22) disclosed figures from pre-lock scripts that were never committed. Three committed blind scripts re-ran them before the lock: library 16/20, context 43/43, power 10/10. **What did not reproduce, or did not describe the declared object:** the ten-signal library's effective rank is **7.97**, not 8.37, which was the twelve-candidate figure; the control turns over **13.6×/yr unscaled and 36.7×/yr held**, not 21.6, which was an eleven-signal blend; P4's 0.338 at α 0.05/6 was one draw on a toy five-signal library over the full 31.6 years, and on the declared object the random-map median is **0.262** (0.178 to 0.361) over 20.0 paired years; P3's clock (8.31/yr, η² 0.048) was one full-sample in-sample fit, and out of sample the declared object runs at **12.55/yr with η² 0.142** against log volatility; P6's swap-repair placebo completes **4 draws in 1,000** on the declared object; the control realises **7.88%**, not 10%, under the cap of 3, which binds on 70.5% of test sessions. **Choices pinned at lock** (§12.0 lists all 36 changes with their direction): the A-1 bar `T_A1 = max(0.338, the fitted pair's MDE at 0.05/6)`; `m_ik` as static-neutralised **mean** contrasts, because a Sharpe contrast would pass on volatility alone; the cost kill anchored to the decision bar at the realised sd, `min(12, T × σ × 500)`, because the version anchored to the uncorrected MDE fired on 4 of 8 content-free maps (the adopted one fires on 0 of 8, and on 1 of 8 once `m` is shaped as §12.5 shapes it), which is EASIER than that version and forced by the measurement; a uniform placebo over join-free orders; volatility witnesses at every level, two of them and read by a test at B-1; C-1 against a state-blind weekly twin under a pooled tracking budget; NOT SHOWN rather than FAIL for a placebo lock with no measured power; the 21-session smoothing that `PLAN.md` §f.4 pre-registered, reinstated as a sensitivity; every level's instrument committed before A is read; a PIT downgrade for any lock that does not hold without the revised NFCI; a PASS labelled "not robust" when a sensitivity at its level has Δ ≤ 0. Declared PIT weaknesses: Ken French files stamped at `period`, and the NFCI read from its current, revised vintage. Three independent audits read the text before the commit; §14 records the four findings resolved differently from their first suggestion. **Sign-off: given.** Guillaume read and approved the text on 2026-09-23, before this commit; a second independent validator and a pre-commit auditor then reviewed it, their blocking findings were applied (§12.0 rows 28-36, and wording), and both confirmed the corrected text before the commit. Any change asked for from here on is logged here as an amendment |
| 2026-09-23 | Two Sigma C-1: power, size, C-2's label and the reading guards | **AMENDED before any reading, no trial spent** — made by the lead of the session that builds the instruments, on Guillaume's instruction of 23/09 to build them, after the instrument audit and before `thresholds.json` was committed. **Finding:** run blind on the real store from the code of `9905eb7`, the level-C instrument gives a C-1 placebo null that is **exactly 0 on all 1,000 draws**, in the primary and in all four sensitivity sections (q20 = q50 = q95 = q99 = 0.0). On content-free labels the pooled-budget λ rule and its training check hold the twin in every fold, so every `S^(j)` is 0. Two consequences under the locked text: `MDE_C = q99 − q20 = 0 ≤ S*` would have declared a power of 0.80 that the location-shift model of §12.10 does not measure, so a C-1 that did not hold would have read FAIL ("does not transfer") on no measured power; and any `S_C > 0`, however small, would have cleared `p ≤ 0.01` (p = 1/1,001). **Ruling:** (a) `powered` requires `0 < MDE_C ≤ S*` **and** no single value taking 20% or more of the draws (`level_c.ATOM_SHARE`); otherwise power is unmeasured and a C-1 that does not hold reads NOT SHOWN, never FAIL. This is the auditor's option (a), widened from a complete atom to a mixed one. (b) **C-1 holds only if also `S_C ≥ S*`**: 7.843×/yr at σ_twin 7.84%, the saving worth 0.05 Sharpe at 5 bp, which is the lock's own declared alternative. Below it C-1 reads NOT SHOWN, or FAIL where power was measured. (c) The instrument also prints the null's sign counts and atom share, which describe content-free draws only. (d) C-2's row logs **BOUND**: the lock gives C-2 no verdict line, and a bound is not a test; p := 1 and "enters no level verdict" are unchanged. (e) Reading guards: every code file a reading runs (`CODE_FILES` in `scripts/run_twosigma_tree.py`) must be committed and identical to HEAD, the reading artifact records HEAD, and a level already in the register is refused before anything is computed. **Also settled by the code before any reading, with no threshold changed:** the trial fields of A-2, B-1 and C-1 (delta = R, G or S_C; threshold = the null's q99; placebo_pct = the statistic's percentile in its null; sharpe NaN where the lock has no arm); sections `A`, `B`, `C` and `<level>:<variant>`; the PIT rebuild also runs for sensitivity rows, at their own K, smoothing and d, and is compared at 0.05/6; witnesses at a sensitivity use its K and are never smoothed; a non-finite witness value makes its lock UNDECIDABLE; the §13.4 level conditions apply to both locks of A and of B (none binds: 14 ≥ 10 cells, 19 of 20 training cells, 7.4% fallback, every pooled matrix positive definite); a non-finite instrument value is written as the string `non-finite`; the NFCI diagnostic aligns each fold's stamped test labels with `align_labels`; at `smooth21` the pooled Σ_f leaves out the unlabelled prefix; a λ tie at an exact floating-point breakpoint returns the literal `h_k(λ_hi)`. **Known before its reading:** the `d = 1.00` sensitivity's kill fires (ΔT 14.20 > 12), so that row will read FAIL (cost), as §12.6 step 3 allows. | The finding is new information, measured by the instrument after the lock, and nothing of the tree had been read, so it could be ruled on without bias. Directions: (a) removes a FAIL that no power supported; it is EASIER on the wording of a non-pass and NEUTRAL on a PASS. (b) is **HARDER**: it closes the one route to a PASS that tested no size, in the programme's rule that an effect below its declared resolution is never a PASS. (c) and (d) are NEUTRAL. (e) is HARDER, because two sessions share this working tree and the bitwise check covered only the instrument's outputs. The instrument audit found nothing else: independent re-implementations of m, R, G with QLIKE, the λ rule and a cadence book match the committed code; the instrument re-runs bitwise identically; its printout stays within §13.1 step 2; and the reading guard refuses. Levels A and B are untouched by this row. |
| 2026-09-23 | Two Sigma level A: the reading's logging crashed, and was finished from its artifact | **INCIDENT, disclosed; nothing recomputed** — the primary reading of level A ran from the committed instruments (`d29de38`) and wrote its artifact `docs/artifacts/twosigma/reading_A.json` with `rows_logged: false`. Appending its first §13.5 row to `data/trials.parquet` then failed: the register's `m_note` column held a float flag (`1.0`, the annotation of an `ahl_level_a` row), and pyarrow refused the text note of the new row. No row was written, and the register was left unchanged. The statistics had not been printed to anyone. **Fix:** `analysis.trials.log` now stores as text any column that holds both text and non-text values, so the old flag reads `"1.0"`, with its content unchanged. `scripts/run_twosigma_tree.py resume-log A` then finishes the reading from its artifact. It runs the same function that ends a reading (`_finish_reading`: log the rows, mark them logged, write the READING section), refuses if anything is missing or already logged, and never recomputes. The artifact records both commits: `git_head` for where the reading was computed, `logging_head` for where its rows were logged. | Recomputing the reading would have been a second reading of level A, which is a protocol breach. Finishing from the stored artifact changes no number and no verdict: the rows and the READING section come from the statistics the reading computed. The register's column-type defect lay outside the Two Sigma code and outside what its instrument audit could see, since no test appended a text note after a numeric one. It now has one. |
| 2026-09-23 | N ≥ 30 sample start settled | **RESOLVED** — 2003-07-17 and 6,039 sessions, the strict reading: 252 sessions **strictly before** t. The permissive reading, counting t itself, gives 2003-07-16 and 6,040 | The frozen §3 names 2003-07-17, and the strict reading is the causally correct one — a signal formed at t−1 cannot use the 252nd session if that session is t. The two readings differ by one session and no result turns on it, but the next study should not inherit the ambiguity. Logged as hygiene item B12 in the shared task list, resolved here. |
| 2026-09-22 | repository layout | **APPLIED** — the three repositories are one. `macro-momentum` and `reversal-lab` are subtree-merged into `chantiers/` with every original commit hash preserved; the roadmap and the five research plans, scrubbed of the private sibling repository and of live-trading figures, are under `pilotage/`. The locked documents are not edited, so they keep citing the old folders; read `../../2_Chantiers_derives/X` as `chantiers/X`, `5_Plans_de_recherche/` as `pilotage/plans_de_recherche/`, `4_Feuille_de_route/` as `pilotage/feuille_de_route/`, and `1_Etude_principale/regime-lab/` as the repository root | Guillaume asked for one repository, current and readable by someone new to the project. Hashes are preserved because this log and the pre-registrations cite them as proof of timing, and a history rewrite into the subfolders would have broken that proof. |
| 2026-09-22 | AHL B1 | **UNDECIDABLE, not read, no trial spent.** The tree-corrected threshold is **0.1265**, which is 49.6% of the book's mean volatility miss (0.255), against a limit of 25% written and committed at `9602526` before the number existed. `scripts/run_ahl_level_b.py`, `docs/RESULTS_AHL_LEVEL_B.md` | §10 names B1's question and not its statistic, so it was operationalised before measurement. The error is how far the M3 headline book misses its 10% target over the next 21 sessions, `|log(RV21/0.10)|`; the break is an accepted CUSUM alarm in the last 21 sessions; the decision t is the smaller of HAC lag 6 and lag 20, because the forward windows overlap; KY becomes a matched placebo, the same detector run on VOL63. Readings: 0.1070 / 0.1156 / 0.1265 at blocks 21 / 63 / 126, and 0.1142 for the rotation null, which is centred at −0.0015. Expectation declared beforehand: undecidable, at 60-75%. The instrument also did not stand where §1 put it: **166 crossings (9.10 a year), which cluster into 48 accepted alarms (2.63 a year) after the dwell**, against the 7.26 disclosed. That figure is the same detector run on log returns, where this instrument uses simple returns; it reproduces exactly on log returns. On log returns the threshold is 52.2% of the mean error, so the verdict does not depend on the return type. The tree goes to C. B2 is not built. |
| 2026-09-22 | AHL pre-lock measurements | **DECLARED, then CORRECTED** — the scripts behind §1 of `PRESPEC_AHL.md` were **never committed**. A first version of this row said they existed nowhere on disk. That was wrong: they were in the temporary scratchpad of an earlier session under `/private/tmp`, which a reboot erases, together with the measurement scripts of all five research plans and of the four A4 counter-expertises. All 120 files were copied on 2026-09-22 to `pilotage/mesures_brutes/`, which is gitignored because almost every file carries absolute paths and the Rentec ones point into a private repository, and to an iCloud backup. Sanitising and committing them is an open task | Fifth instance of the programme's signature failure. Before the files were found, VOL63 had been re-pinned by reproducing a disclosed figure. The original script confirms the definition, the cross-sectional median of the 46 instruments' 63-session volatility, but computes it on **log** returns. So does the correlation distance, and that is the whole of the gap between the 7.26 alarms a year disclosed in §1 and the 9.10 counted by `run_ahl_level_b.py` on simple returns. The original CUSUM standardisation is causal, like the one used here. |
| 2026-09-22 | AHL A1 fast leg | **DECLARED, unresolved** — A1 was read and logged on F′ = (63, 5). §4 of the locked text names **F = (126, 10)** as the headline fast leg and (63, 5) as a declared sensitivity | Found while building level B. `run_ahl_panel_power.py` and `run_ahl_level_a.py` both fix `FAST = (63, 5)`, and the 2.12-point threshold was measured on it. §4 chose (126, 10) on cost grounds before any return was seen. The published refutation is therefore a refutation of the sensitivity leg; the headline reading is still owed, and its MDE must be measured before it is read. It was not run here, because running it is a decision about the tree and not a repair. |
| 2026-09-22 | dates | **NOTED** — the rows dated 2026-09-23 in this log and in `PRESPEC_AHL.md` were written on 2026-09-22, as the commit timestamps of `630ee16`, `92e4e9e` and `f5b70c1` show | The ordering of the rows is right; the day is off by one. Nothing turns on it, but this log is the chronology other documents cite. |
| 2026-09-23 | AHL panel power measured | **INSTRUMENT CHECK, no trial spent** — the minimum detectable difference-in-differences of hit rate on the 46 × 5,786 panel is **1.33 points**, against a criterion of 4.5 written before the number was produced. `scripts/run_ahl_panel_power.py` | The first power check in this programme to come back favourable. Stationary bootstrap on the date dimension, whole cross-section preserved per draw, gives 1.27 / 1.30 / 1.33 points at blocks 21 / 63 / 126; the rotation null, which preserves the state's run structure and destroys only its alignment, gives 1.27 and is centred at −0.022 points. The plan's own analytical bracket was 0.8 (clustering by date) to 4.5-6.0 (clustering by 63-session episode) with a declared expectation of 2 to 4; the measurement lands at the date end, which says the signal × return product really is close to white. The observed statistic is deliberately not read here: reading it is level A and level A is a trial. **Declared against itself:** the first run of this script returned NaN across every reading and printed "not decidable", which was a broken instrument reading as a result — `(ratio > median).shift(1)` yields object dtype, where `~True` is −2 rather than False, and the weight totals went to −367,992. Fixed, and the script now refuses to issue any verdict when a reading is not finite. |
| 2026-09-22 | scope of the regime conclusion | **NARROWED** — "the regime line closes" is replaced by what the six measurements license: a classifier **ordered by training volatility**, changing state **0.514 times a year**, used as a **sizing multiplier or a switch at portfolio level**, on a **single trend book of effective dimension 3.86**, adds nothing beyond a one-line volatility quantile | The six refutations never varied four axes: the latent variable, the time constant, the use, and the conditioned object. Five pre-registerable plans built on documented practitioner constructions each cross at least one — Man AHL's change-point detector produces **11.04 ruptures a year against our 0.514**, Bridgewater uses the quadrant for **portfolio construction** rather than prediction, Two Sigma uses regime for **selection between signals**, a question with no meaning on one signal. The generalisation was mine and it went past the measurement; the measurements themselves are unchanged. Plans in `5_Plans_de_recherche/`, outside this repository. |
| 2026-09-22 | §7 financing corrected | **APPLIED** — `FUNDED_ON_CASH` is separated from `NO_FUTURES_VEHICLE`. All nine bond and credit ETFs are funded at the 3-month cash rate; previously only six were | A defect in this repository, not a reading of the frozen text. `fetch_trend_universe.py:23` downloads with `auto_adjust=True`, so every ETF column is a **total return** series. `NO_FUTURES_VEHICLE` was built for §6 by a property of the market — an IEF exposure trades as ZN and pays the futures rate — then reused for §7, where the question is what the series already contains. IEF, SHY and TLT fell through `cost_class`'s default branch and were never charged, while six identical bond ETFs were. SHY drifts at 1.92%/yr against a mean cash rate of 1.77%. Worth **0.041 of Sharpe** on the headline, and it closes the study's own leading reservation in the unfavourable direction: the §3-faithful construction missed the 0.50 line by 0.005 and now misses it by 0.039. Covered by a test asserting the two tuples answer different questions. |
| 2026-09-22 | placebo claims withdrawn | **WITHDRAWN** — "both placebos fail to distinguish anything", and P3 as a test at all | P1's 88.5th percentile is a statistic of the first 63 sessions: `unscaled_weights` returns zeros before the book is live, σ is estimated on zeros, and the multiplier explodes. The 43.8× maximum gross is that artefact and falls to 7.96 once those sessions are dropped; on the purged sample P1 reads **99.8%**, above the 95th percentile. And P3 cannot fail by construction — Sharpe is scale-invariant to 1.1e-16, so its two lines are K3 and K4 recomputed. Neither withdrawal changes a verdict; both change what the study may claim. |
| 2026-09-22 | K5 PASS not established | **DOWNGRADED** — K5 is reported as *not established* rather than as a PASS | The deflation collapses `trials.parquet` to one Sharpe per configuration by taking the **mean**, which compresses the dispersion 4.9-fold — and that dispersion is the quantity deciding K5. Across four defensible collapses the deflated excess runs from −0.159 to +0.286 and crosses zero. §12 fixes the trial *count* and is silent on the variance estimator, and the reading of "DSR ≤ 0" was settled after both readings had been seen. No consequence for H, which K1 kills; consequence for the §9 gate, which requires DSR > 0. |
| 2026-09-22 | headline construction | **DECLARED, unresolved** — the committed construction slices the sample before building the signal and reads +0.359; the construction §3's own wording implies reads +0.461 on 6,039 sessions | Both fall below the 0.50 kill line and neither approaches the 0.70 gate, so no verdict turns on it. The headline was **not** moved to the arm that reads higher after seeing that it reads higher — that is the adjustment this protocol exists to forbid. The frozen K6 figures, the turnover and the rotation were all measured on the committed construction. Left for the next pre-registration to fix at the point where it can be fixed blind. |
| 2026-09-21 | leverage cap read | **RESOLVED** — "portfolio leverage cap 3.0" in §5 is applied to the **multiplier**, the quantity its named source caps, not to the book's gross exposure. Measured in `scripts/run_m3_construction.py` | The two readings do not merely differ, they disagree completely: the cap binds on **0.00%** of sessions on the multiplier and on **52.24%** on gross exposure, because median gross lands at 3.080, all but on the threshold. The named source settles it — `MAX_LEVERAGE` in `extensions/trend.py` clips the per-instrument multiplier `(VOL_TARGET/σ)`, never a gross. And K6's own rule disqualifies the other reading: a cap binding on more than 5% of sessions is a design failure, and capping gross at 3.0 pulls realised volatility from 10.90% to **9.14%**, so M3 would stop hitting the target it exists to hit. Declared against itself: under the reading taken the cap is **inert**, and the book's gross exposure runs at a median of 3.08, a p95 of 5.30 and a maximum of **43.8** on 2004-07-07, where a 63-session volatility estimate collapsed. Eight sessions exceed 10× and one exceeds 40×. "The cap never binds" would otherwise read as reassurance rather than as the absence of a constraint. |
| 2026-09-21 | cost class read | **RESOLVED** — §6's "any instrument kept on a non-futures vehicle" is read as a property of the market, not of which series sits in the cache: an S&P exposure is priced as a future whether or not this repository holds the ES series, and an LQD exposure has no futures contract at any price. Eight of the 46 pay the cash rate: NOK, SEK, AGG, BWX, EMB, HYG, LQD, TIP | The M2 narrowing left every instrument on its stored series, so a literal reading would price all 46 at 7.5 bp and erase the distinction §6 exists to draw. That is also not what D2 measured when it priced the book all-futures at 1.0 bp, which is the measurement that motivated this study. The literal reading is not discarded: it is reported as the `ALL_CASH` schedule, the most conservative available, so the span is visible rather than chosen. |
| 2026-09-21 | turnover calibration | **DECLARED, no change** — §6 calibrated the expected turnover rise at 15.15× to 24.71× per year. Measured on this construction: **15.36× to 58.92×** | §6's reference device scaled a book whose own volatility already sat near its target, so its multiplier hovered near one. M3 levers a book realising 4.13% to a 10% target, so it runs a median gross of 3.08 and pays for that notional. §6 states that higher turnover is expected and is not a reason to alter the construction, so the construction is unaltered and the gap against the calibration is reported rather than absorbed. It matters because cost is charged per instrument and eight instruments pay 7.5 bp. |
| 2026-09-21 | M2 narrowed | **APPLIED** — the price-series substitution of M2 is demoted to a sensitivity over the window where it is possible, and can never be the headline. The headline keeps the stored series for all 46 instruments over the locked 23.2-year sample, flags every one of them, and applies the vehicle as a **cost schedule**, which is what D2 measured and what makes the verdict flip. Measured in `scripts/audit_vehicle_coverage.py` | Substituting is possible for 10 of 46 instruments, and the intersection of their availability is 2017-07-10 to 2026-06-12: **8.9 years**, because RTY starts in 2017 and the other nine in 2012. At 8.9 years the smallest resolvable Sharpe is **1.253** against this study's own 0.70 gate, so substituting would make the headline hypothesis undecidable. Substituting each instrument over its own window instead would splice a stored series to a futures series mid-sample, injecting a join discontinuity — the class of defect M2 exists to remove. And the locked text's "without flagged instruments" variant would hold 10 instruments, below the N ≥ 30 rule and far below the effective breadth of 11 to 21 that §3 already declares; it is reported as breadth-underpowered rather than as a pass or a fail. This is the truncation trap the programme has already fallen into twice, where the apparent signal WAS the truncation. Like the M1 narrowing, it makes the modification smaller, was measured before the repaired book produced any return, and is logged rather than edited into the frozen text. |
| 2026-09-21 | D3(b) corrected | **NARROWED** — D3(b) named GC=F, SI=F and CL=F as raw front-month series carrying roll gaps. **GC=F and SI=F are**, at correlation 1.0000 with the raw front month, costing 1.02 and −0.18 points of annualised drift and flipping the sign of a 12-minus-1 trend signal on 7.5% and 5.6% of sessions. **CL=F is not**: it correlates 0.9671 with the back-adjusted series against 0.9252 with the raw one | The claim was inferred from one measurement on the raw column of a sibling series rather than on the stored series itself. Ten further commodities carry the same defect with no back-adjusted reference, so its size there is unknown rather than zero — which is the reason the headline flags all 46 rather than claiming any of them repaired. |
| 2026-09-21 | M1 scope confirmed | **NO CHANGE** — the one-session lag is a property of the `=X` spot endpoint and of nothing else. Equity indices correlate 0.9717 to 0.9766 same-day against their futures, commodities 0.9671 to 0.9901, and IEF 0.9256 against ZN; all show a negative lagged correlation | `audit_fx_alignment.py` bounded M1 to the FX family by testing only the FX family. That left open whether other families shared the defect. They do not, and saying so by measurement rather than by silence is what lets the eight-series shift stand as the whole of M1. |
| 2026-09-21 | new study | **OPENED** — `docs/PRESPEC_TREND_VEHICLE.md`, locked before the repaired book produced a return. The 46-instrument trend book becomes the *subject* rather than the vehicle: three modifications and no others (realign the ten spot FX series, back-adjust GC/SI, apply a portfolio-level volatility target), with regime state entered as one covariate in the risk budget and never as a switch | Audits of the published artefacts found the reference book of every comparison in `EXTENSIONS.md` is gross, unsized, and cost-fragile: gross turnover 14.79×/yr against a 15.7 bp breakeven, so this programme's own cost schedule takes 48.8% of the gross return and the net Sharpe is 0.261. Ten spot FX series are stamped one session late — corr(t+1,t) 0.877-0.896 against the matching CME futures, confirmed on the Brexit session — and GC=F/SI=F correlate 1.0000 with raw front-month, so roll gaps are counted as returns. And the winner of `EXTENSIONS.md` is **vehicle-dependent**: +0.244, t +2.91 on futures at 1 bp, but +0.106, t +1.23 on the retail mix implied by the stored universe, under its own MDE. None of the five refutations tested the construction of the book; all tested a conditioner for which it was the vehicle. The non-blindness is disclosed in §1 of the pre-registration: the futures vehicle was chosen after seeing that cost sensitivity. |
| 2026-09-14 | `external_validation` | **APPLIED** — the function now returns NaN when either the state or the reference has fewer than two distinct values, instead of scoring the degenerate case; every branch returns the same six keys, where the short-circuit branch previously returned three. Covered by `tests/test_reliability.py` | A defect with two halves, and the larger half was not the one first noticed. A constant state satisfies `state == state.min()` everywhere, so the classifier was graded as if it had called a recession every day — three per-fold cells read 0.0%. A **one-class reference** has no balanced accuracy at all: the mean collapses onto the recall of the only class present and kappa's denominator vanishes, returning 0.000 indistinguishably from real incompetence. That affected 15 of 25 per-fold cells, three of which read *high* — A jump showed 75.6% and 98.0% in folds 0 and 4, C gradient boost 16.8% in fold 4, all of them specificities presented as balanced accuracies. Six misleading cells, three low and three high, from one defect with opposite signs. **No published figure changes**: every published number is full-sample, where both series move, and A′ sparse jump stays at 0.9316 / 0.5342. NBER fires in only 2 of these 5 folds, so per-fold external validation is structurally impossible on this sample and the honest table has two rows. |

## Deviations from the frozen text

The charter is not edited to match reality; deviations are recorded here.

| item | charter says | actual | why |
| --- | --- | --- | --- |
| tag signature | "signed git tag pushed publicly" | annotated tag, unsigned, pushed publicly | No signing key is configured on the author's machine. The property that matters for a third party is the **push date**, which GitHub records independently of the signature. To be upgraded if a key is set up; the tag will be re-cut and this row updated. |
| stopping rule 1 | a positive gap above the MDE on three folds of five | uncomputable for A and A′; **0 of 5 for every family** on the folds that are identified | Two of A′'s five folds hold one state, so three of five cannot be assessed for it. Where it can be assessed, no family clears the per-fold MDE in any fold: per-fold gaps swing from −0.71 to +1.01 against MDEs of 0.35 to 1.36, so the fold-level test has no resolution. Reported in `docs/RESULTS_FALSIFICATION.md` §T1 and `docs/RESULTS_FOLDS.md` §2. |
| T1 timing | a control run before the primary results | run after them, 2026-09-13 | The control was not implemented during the study. It is specified from the charter text, its sensitivities were declared before its own primary output was read, and it produced a result that weakens the study. Recording the order is the only defence available after the fact. |


## Correction — the inversion was a sign error here, not a convention there

The first version of this log explained the inverted labels as a property of the
reference implementation: that the volatile state had earned more cumulatively
in training, so ranking by cumulative return put it first. **That explanation is
wrong, and the repository's own data contradicts it.**

Refitting with `ordering="cumret"` on the training window through March 2002:

```
state 0 : cumulative return +0.363   volatility 0.0050   1,607 days   (the CALM state)
state 1 : cumulative return +0.167   volatility 0.0079   1,023 days   (the VOLATILE state)
```

The volatile state earned **less**, not more. The real cause is that
`jumpmodels` sorts **descending** — index 0 carries the highest cumulative
return — while `mapping.py` documented the opposite ("states arrive ordered by
the return earned in them, so the strongest is the last index") and held
`n_states - 1`. The wrapper was long the weakest state by construction.

The filtered HMM sorted ascending, correctly, which is why the two families
disagreed at kappa −0.39. That gap was never a difference of convention between
methods; it was one bug in one wrapper.

The fix stands — ranking by volatility never touches returns and cannot invert —
but the justification recorded here was counterfactual, and an amendment log
that misstates its own reason is worse than no log. Anyone can reproduce the
three lines above in a single fit.
