[project]
name = "macro-momentum"
version = "0.1.0"
description = "Do changes in macro conditions predict cross-asset returns, where levels do not?"
requires-python = ">=3.12"
dependencies = [
    # This project imports regime_lab for the point-in-time contract, the
    # coverage guard, the stationary block bootstrap and the trial register.
    # Resolved from the workspace root since the repositories were merged.
    "regime-lab",
    "pandas>=2.2", "numpy>=1.26", "pyarrow>=16", "requests>=2.31",
    "yfinance>=0.2.40", "python-dotenv>=1.0", "scipy>=1.13",
    "statsmodels>=0.14", "scikit-learn>=1.5", "openpyxl>=3.1",
]
[project.optional-dependencies]
dev = ["pytest>=8.0", "ruff>=0.6"]
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
[tool.hatch.build.targets.wheel]
packages = ["macro_momentum"]
[tool.ruff]
line-length = 100
target-version = "py312"
[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.uv.sources]
regime-lab = { workspace = true }
